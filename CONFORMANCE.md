# Lighthouse — Conformance self-description

*Self-attested; seed stage. Registry IDs: `JFA/jfa-conformance-suite.py`. Lighthouse declares its own seven-point standard in `lighthouse-seed.md` ("The standard"); this file maps that standard onto the architecture registry and states honestly what is designed vs. built (nothing is built yet).*

**Shape:** an education-layer witnessed knowledge commons (Part VI), carried by civic hubs that each hold the coordinator and witness roles — pointed outward, never witnessing their own log.

| ID / discipline | Status |
|---|---|
| No truth authority (Part VI) | **Met by design** — flag never finding, anchor never certify; occurrence-never-truth at T4; no verdict from any hub, center, scan, or model |
| Citation legible and forkable (Part VI) | **Met by design** — typed discovery graph, provenance rendered, artifacts in the AGPL content-addressed commons |
| Reputation earned, never sold (Part VI) | **Met by design** — no paid placement; attestation is a service never a purchasable rank |
| Claims anchored as claims (Part VI) | **Met by design** — the bound triple (fact/procedure/result) anchors inputs and outputs, never a verdict |
| COV-1 no averaging | **Met by design** — full distribution; concordance held per category ("consistent at four hubs, inconsistent at one"); citation counts size nothing permanently |
| COV-3 symmetric | **Met by design** — every anchor answerable; challenges are first-class edges; dismissals annotate; breach annotates never erases |
| REC-1/3 append-only, PII-free | **Met by design** — structural facts and hashes only; artifact-dark instead of erasure; no PII in the commons |
| REC-6/7/8 witnessing, filing upstream | **Designed** — no institution witnesses its own log; harm files at a neighboring hub; two-hub federation floor with stand-in label below it |
| COV-6 Sybil before citation-weight | **Met by design (by ordering)** — weight sizes nothing until a governed witnessed switch; corroboration counts hubs not accounts; physical presence is the per-unit cost |
| ECO-2/4/5 economy inflow | **Designed** — eligibility never sizing; earned-never-bought, non-redeemable; regulatory read gates the switch |
| GOV-9 living open-questions document | **Met** — `OPEN-QUESTIONS.md` extracts Part IX; the seed's Part IX is the prose source |
| GOV-10 copyleft commons | **Met** — AGPL-3.0 (code), inbound = outbound, no CLA |
| Problem 2 / Stage 0 | **Named** — nothing above T1 reads honestly until the federated two-hub record is real; all standing is self-attested until then |

**Honesty line:** Lighthouse is a seed — every row above is design, not running code. Its hub-as-witness construction is a *proposed* lever on architecture problem 8, to be proven in deployment, not assumed. Launch floor is two independent hubs; anything less ships under the stand-in label.
